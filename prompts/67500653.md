Analyze the provided logs for a multi-stage attack. This offense triggers when Initial Compromise events (e.g., Malware, Exploit) are followed by Post-Compromise Activity (e.g., AD group changes, Botnet C2, reconnaissance) within 1 hour on the same IP.
Apply these specific heuristics to map your findings to the scoring rubric:
1. Map to 0.0 - 0.3 (CLEAR FALSE POSITIVE) if the Initial Compromise events show a 'Security_Action' of 'Blocked', 'Cleaned', or 'Dropped' (the AV/Firewall stopped the threat), AND the subsequent Post-Compromise events appear to be unrelated, benign administrative tasks or routine traffic. Verdict: 'Contained_False_Correlation'.
2. Map to 0.4 - 0.6 (SUSPICIOUS BUT INCONCLUSIVE) if the Initial Compromise was logged but the 'Security_Action' is unknown or 'Allowed', yet the Post-Compromise activity is ambiguous (e.g., generic scanning). Verdict: 'Suspicious_Investigation_Needed'.
3. Map to 0.9 - 1.0 (CONFIRMED COMPROMISE) if the Initial Compromise events were 'Allowed' or successful, AND they are clearly followed by highly malicious Post-Compromise actions from the same host (e.g., unauthorized AD membership changes or successful C2 connections). Verdict: 'Active_Multi_Stage_Compromise'.