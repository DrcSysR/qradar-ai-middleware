"Look for brute-force attempts against user accounts. Check for successful logins following the failures. If success is found, set score > 0.9 and verdict 'Compromised'. If only failures are present, set score <= 0.3 and verdict 'Benign'."