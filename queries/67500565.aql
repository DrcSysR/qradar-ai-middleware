SELECT 
    QIDNAME(qid) AS Event_Name, 
    username AS Admin_User, 
    sourceip AS Source_IP, 
    destinationip AS Target_IP, 
    "Logon Type" AS Logon_Type,
    "Process Name" AS Process,
    COUNT(*) AS Event_Count,
    UNIQUECOUNT(destinationip) AS Unique_Targets
FROM events 
WHERE 
    INOFFENSE({offense_id})
GROUP BY Event_Name, Admin_User, Source_IP, Target_IP, Logon_Type, Process
ORDER BY Event_Count DESC
LIMIT {limit} {time_depth}